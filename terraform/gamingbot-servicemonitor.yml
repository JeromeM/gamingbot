apiVersion: monitoring.coreos.com/v1
   kind: ServiceMonitor
   metadata:
     name: gamingbot-monitor
     namespace: gamingbot-namespace
     labels:
       app: gamingbot
   spec:
     selector:
       matchLabels:
         app: gamingbot
     endpoints:
     - port: http
       path: /metrics
       interval: 15s